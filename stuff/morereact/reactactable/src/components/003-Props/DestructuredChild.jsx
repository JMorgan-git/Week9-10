//just pullinh in what it sees without imports
const DestructuredChild = ({propData}) => {
    return ( 
        <>
            <h2>Text with no imports: {propData}</h2>
        </>
     );
}
 
export default DestructuredChild;